<!-- <div class="col-md-6 col-md-offset-3">
  <ul class="nav nav-pills" role="tablist" id="tab">
    <li class="active"><a href="#drawPane" role="tab" data-toggle="tab">Draw!</a></li>
    <li><a href="#describePane" role="tab" data-toggle="tab">Write!</a></li>
  </ul>

  <div class="tab-content">
    <div class="tab-pane active" id="drawPane">
      <%= render 'canvas' %>
    </div>
    <div class="tab-pane" id="describePane">
      <%= render 'new_game_describe_form' %>
    </div>
  </div>
</div> -->

<div ng-app="completedGames" ng-init="tab = 1">
  <div class="col-md-6 col-md-offset-3">
    <ul class="nav nav-pills">
      <li ng-class="{active:tab === 1}"><a href ng-click="tab = 1">Draw!</a></li>
      <li ng-class="{active:tab === 2}"><a href ng-click="tab = 2">Write!</a></li>
    </ul>

    <div class="tab-pane" ng-show="tab === 1">
      <%= render 'canvas' %>
    </div>
    <div class="tab-pane" ng-show="tab === 2">
      <%= render 'new_game_describe_form' %>
    </div>
  </div>
</div>